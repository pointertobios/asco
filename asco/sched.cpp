#include <asco/sched.h>

namespace asco::sched {

    std_scheduler::std_scheduler() {
        
    }

    void std_scheduler::push_task(task t) {
    }

    std::optional<std::coroutine_handle<>> std_scheduler::sched() {
    }

};
